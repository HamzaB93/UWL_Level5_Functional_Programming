#lang racket

;; Introduction to scheme

;; 1) Functions

; a) Area of a circle
;(define (area r) (* 3.14 (* r r)))

; b) A better Area function
;(define PI 3.14)

;(define (area r)
;  (* PI (* r r)))

;; Tasks

; a) Dollar to euro
;(define EURO 0.92)

;(define (d-t-e d)
;  (* d EURO))

; b) Celcius to fahrenheit
